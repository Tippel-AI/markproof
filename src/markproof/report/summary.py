# SPDX-FileCopyrightText: 2026 Lukas Friedrich / Tippel
# SPDX-License-Identifier: Apache-2.0
"""Markdown summary for ``$GITHUB_STEP_SUMMARY``.

Part of the default output path (Auflage A2): zero system dependencies, always
producible. Renders the PASS/FAIL/WARN/SKIP counts, the failing rules with their
Guidelines reference, the rulepack version with its attribution line, and the
signature fingerprint.
"""

# TODO(M4): render(report) -> str; write to $GITHUB_STEP_SUMMARY when present.
