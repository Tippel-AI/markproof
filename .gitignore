# SPDX-FileCopyrightText: 2026 Lukas Friedrich / Tippel
# SPDX-License-Identifier: Apache-2.0

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
sdist/
wheels/
*.egg-info/
.eggs/
.installed.cfg
MANIFEST

# --- Environments -----------------------------------------------------------
.venv/
venv/
env/
.python-version

# --- Tooling caches ---------------------------------------------------------
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.hypothesis/
.tox/
.nox/
.cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
site/

# --- Editors / OS -----------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store

# --- markproof runtime output ------------------------------------------------
# Reports and evidence are build artefacts, produced per run, never committed.
report.json
report.pdf
artifacts/
.markproof/

# --- Secrets ----------------------------------------------------------------
# Signing keys, endpoint tokens and watermark configs come from the environment
# or CI secrets. If one of these ever appears here, something went wrong.
.env
.env.*
secrets/
*.pem
*.key
watermark_config*.json
# ...except the demo bot's, which is test material, not a secret: throwaway keys
# for a bot that serves fixed strings. Without it the demo and the dogfood
# workflow cannot run, and a rule that silently breaks the example it ships with
# protects nothing. It carries `.demo` in its name so that the marking survives
# being copied out of here — the config schema forbids unknown fields, so the
# file cannot say it in a comment of its own.
!examples/demo-bot/watermark_config.demo.json

# Reports written by a local run. The tool writes one by default now, so an
# accidental `markproof run` in the checkout must not become a commit.
markproof-report/
